{
  "name": "alpha-auto-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsc --watch --pretty",
    "build": "tsc --pretty",
    "test": "vitest",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@types/chrome": "^0.0.249",
    "@types/node": "^20.11.30",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  }
}
